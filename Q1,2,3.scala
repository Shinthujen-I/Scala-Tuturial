//Question 1,2,3
object  Question {
    def main(args: Array[String]): Unit = {
        var k = 2;
        var i = 2;
        var j = 2;

        var m = 5;
        var n = 5;
       
        var f :Float = 12.0;
        var g :Float = 4.0;

        var c :Char = 'X';

        println(k + 12 * m);
        println(m / j);
        println(n % j);
        println(m / j * j);
        println(f + 10 * 5 + g);

        i += 1;
        println( i * n );
    }
}
//In java we can use increment and decrement.
// But in scala we cant use unary operaors